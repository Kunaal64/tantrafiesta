/**
 * Loading Component
 * 
 * A simple full-screen loading indicator.
 * Displays a "Loading" text centered on the screen.
 */
const Loading = () => {
  return (
    <div className="absolute top-0 left-0 h-full w-full flex justify-center items-center text-xl">
      Loading
    </div>
  );
};

export default Loading;


